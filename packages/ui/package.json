{
  "name": "@flowforge/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "vue-tsc --noEmit -p tsconfig.json",
    "type-check": "vue-tsc --noEmit -p tsconfig.json",
    "check": "npm run lint && npm run type-check"
  },
  "devDependencies": {
    "@flowforge/types": "file:../types",
    "typescript": "^5.9.3",
    "vue-tsc": "^3.2.5"
  }
}
